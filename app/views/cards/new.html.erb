<% title "Create New Card" %>

<% form_for(@card, :url => project_cards_path(@card.project_id)) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :locals => {:form => f, :card_statuses => @card_statuses} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', project_path(@card.project_id) %>